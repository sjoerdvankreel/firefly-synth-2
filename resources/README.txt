Install:
* Delete the previous installation from your plugin directory (all of it, not just the plugin binary).
* Copy the entire plugin folder (.vst3/.clap) to your plugin directory (all of it, not just the plugin binary).