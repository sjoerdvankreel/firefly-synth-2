v2.1.1
[x] limit string osci from 20->8hz low + clamp
[x] allow custom font and fontsize
[x] get rid of the header
[x] something with the image
[x] externalize header.png
[ ] do all kinds of non-breaking stuff, break later
[ ] do more easy access controls (and maybe more mod sources)
[ ] bpexpo envelopes + docs
[ ] click main module graph shows details
[ ] not allcaps for graphs
[ ] osc mod to own row (single)
[ ] voice/master/settings to tabbed single row
[ ] matrix/unison as popups?
[ ] rework unison to matrix format (breaking) with same targets as voice matrix
[ ] get rid of graphschemefollowsmodule
[ ] update the manual for about box
[ ] fixup all the other themes
[ ] document all the theme changes
[ ] fix the 1sec delay on plot visuals
[ ] darken the bg when overlay/filepicker is active
[ ] make some knobs stand out (2-row) like master PB
[ ] do a second row of graphs, main on top 1 for each module, detail on botton N for active main
[ ] just auto-fillup the last grid
[ ] up the margins
[ ] alternate-color the matrix rows et all
[ ] pretty knobs, graphs, combos, checks, buttons, more?
[ ] programmatic dump screenshot?

fix the string oversample bug already
drop screenshot_theme_light_default (for the website) once fix the auto-screenshot
also probably make the images on the website bigger somehow
allow more margin/padding, the borders clip
allow full fx module inside the feedback delay
dedicated distortion with routing like the echo
do display disabled/impossible items different in the mod matrix
VDist/GDist - bitcrush, downsample, filter, eq, shape, oversample, more?
VComp/GComp - ext audio in, but can be other stuff as well

pck support
arpeggiator
allow more stuff on the feedback delay path, like, try to get an entire fx block in there somehow => just put one in a popup window?
built-in compressor (can actually do that now with side-chain in, so nice)
more presets
fancy patch browser
check into AV Linux
arpeggiator/sequencer
vst3 note expr + midi mpe
dedicated distortion module
split large matrix dropdowns
architecture/developer document
percussion/porta lead demo patches
string timbre should not react to samplerate/oversample
separate midi, bpm, automation smoothing, auto should be global + per-param override